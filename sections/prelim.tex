\section{Preliminaries}

\paragraph{Notation.}
We use $\lambda \in \mathbb{Z}$ to denote the security parameter.
We use $x \gets y$ to denote the assignment of the value of $y$ to $x$,
and write $x \rgets S$ to denote sampling an element from the set $S$ independently and uniformly at random.
For a randomized algorithm $\adv$ we write $y \rgets \adv(x)$ to denote the random variable
that is the output of $\adv(x)$ on input $x$.
%
We use $\set{n}$ for the set $\{1,\ldots,n\}$.
%
For a positive integer $q$ we use $\Zq$ to denote the ring $\Z/q\Z$.

When writing protocols,
$\require$ denotes the clause that if some predicate~$\cdot$ does not hold,
then the respective party must abort the protocol.

\subsection{Polynomial Interpolation and Shamir Secret Sharing}
\label{prelim:secret-sharing}

A polynomial $f(x) = a_0 + a_1 x + a_2 x^2 + \ldots + a_{\tcor} x^{\tcor}$
of degree $\tcor$ over a field $\F$ can be interpolated by $\thresh$ points.
Let $\eta \subseteq \set{n}$ be the list of $\thresh$ distinct indices
corresponding to the $x$-coordinates $x_i \in \F$ of these points.
Then the Lagrange polynomial $L_i(x)$ has the form:

\[
  L_i(x) = \prod_{j \in \eta; j \neq i } \frac{x-x_j}{x_i - x_j}
\]


Given a set of $\thresh$ points $(x_i, f(x_i))_{i \in \eta}$,
any point $f(\xtilde)$ on the polynomial $f$ can be determined by Lagrange interpolation as follows:
\[
  f(\xtilde) = \sum_{k \in \eta} f(x_k) \cdot L_j(\xtilde)
\]

\paragraph{Shamir Secret Sharing.}
Shamir secret sharing~\cite{Shamir79} is an $(n, \thresh)$-threshold secret sharing scheme consisting of algorithms $(\IssueShares, \allowbreak \Recover)$, defined as follows:

\begin{itemize}[itemsep=1mm]
    \item $\IssueShares(\secret, n, \thresh) \rightarrow \{ (1, \share_1), \ldots, (n, \share_n)\}$:
      On input a secret $\secret$, number of participants $n$, and threshold $\thresh$, perform the following.
First, define a polynomial $f(Z) = x + a_1 Z + a_2 Z^2 + \cdots + a_{\tcor} Z^{\tcor}$ with $a_1, \ldots, a_{\tcor} \randpick \Z_p$.
Then, set each participant's share $\share_i, i \in \set{n}$, to be the evaluation of $f(i)$:
    \[
        \share_i \gets \secret + \sum_{j \in \set{\tcor}} a_j i^j
      \]
%
    Output $\{(i, \share_i) \}_{i \in \set{n}}$.

  \item $ \Recover(\thresh, \{ (i, \share_i)\}_{ i \in \coalition}) \rightarrow \bot / \secret$:
    On input threshold $\thresh$ and a set of shares
$\{(i, \share) \}_{i \in \coalition}$,
    output $\bot$ if the coalition $\coalition \not\subseteq \set{n}$ or if
$\lvert \coalition \rvert < \thresh$.
Otherwise, recover $\secret$ as follows:
  \[
      \secret \gets \sum_{i \in \coalition} \share_i \cdot L_i(0)
  \]
    where the Lagrange coefficient for the set
$\coalition$ is defined by
    \[
    L_i(0) = \prod_{j \in \coalition, j\neq i} \frac{j}{j - i}
    \]
\end{itemize}


\subsection{Communication Model}
\label{sec:comm-model}

When describing distributed key generation schemes,
we assume each pair of parties can communicate over a secure channel.
We introduce below two types of secure channels that we rely upon for our DKG construction.


We also assume a synchronous model of communication;
which means we require that protocol execution progresses in well-defined rounds, with every message sent in a given round being delivered before the start of the subsequent round.
We assume that the adversary can be rushing, meaning that in any given Round~$r$ the corrupted parties can receive all messages sent by the honest parties in Round~$r$ before the corrupted parties are required to send their Round-$r$ messages.

\paragraph{Broadcast Channels}
A broadcast channel sends one message to all other players,
with the guarantee that all players received the same message.
A broadcast channel guarantees the properties of authenticity, reliability, and consistency.
We denote sending over a broadcast channel with $\sendbroadcast$. Note that on the MPC level,
these channels are equipped with timeouts.

Concretely,
we instantiate $\sendbroadcast$ with the ``Authenticated Double-Echo Broadcast'' protocol
from~\cite{CGR11} in order to achieve Byzantine reliable broadcast. A reliable broadcast
channel specifically guarantees the properties of Validity, No duplication, Integrity
and totality (see Module 3.11 and Module 3.12 of~\cite{CGR11}).
The file \textit{echo\_broadcast.rs} contains the implementation of the reliable broadcast protocol.
We essentially follow the same algorithm 3.18 of~\cite{CGR11} with three mild differences:
\begin{enumerate}
\item If a party is broadcasting a value, then the protocol expects that other parties would also want to broadcast some values. This is essentially equivalent to running $n$ parallel instances of algorithm 3.18 where one instance where party $k$ is a sender and $n-1$ instances where $k$ is a receiver. Such an implementation serves directly our use cases in protocols where symmetry holds between participants. Additionally, this implementation prevents having deadlocks (two parties waiting for each others to send values on two different broadcast channels) and prevents having inefficient sequential broadcast instances.
\item If an (honest) broadcast sender recovers an output at the end of the protocol that is different than
the value it input, then it aborts considering that the effective number of malicious parties is larger than what the
protocol allows.
\item If a party collects a number of votes and deduces that there is no way to meet the necessary threshold for any of
the collected votes, then it immediately aborts instead of waiting for timeouts to abort the process.
\end{enumerate}

\subsection{Distributed Key Generation}
\label{def:dgk}

We follow the definition and idealized notion of security $\FDKGAbort$
for a distributed key generation (DKG) protocol where the adversary is allowed to bias the output key with some (constrained) additive  shift $\Delta$,
as presented by Katz~\cite{Katz23}.
We give a stand-alone definition of security for $\FDKGAbort$.
Because our proof for $\pedpopcomm$ uses only online straight-line simulation,
our proof with respect to $\FDKGAbort$ in the standalone setting is also by extension secure in the Universal Composability (UC) framework~\cite{KushilevitzLR10}.
We assume an adversary that performs only static corruptions.


\begin{figure}[H]
  \begin{framed}
    \begin{center}
      $\FDKGAbort$
      \begin{enumerate}
        \item Let $n, t$ be parameters provided to $\FDKGAbort$.
        \item Send $(n, t)$ to the adversary $\adv$; receive $\corrupt$ from $\adv$.
        \item Let $f$ be a degree $t$ polynomial randomly sampled from $\Zq$.
        \item Compute $\secretComm \gets g^{f(0)}$
        \item Compute $(\SK_i,\PK_i) \gets (f(i), g^{f(i)})$ for all $i \in \set{n}$.
        \item Send $(\secretComm, \set{\PK_i}_{i\in\set{n}}, \set{\SK_i}_{i\in\set{\corrupt}})$ to the adversary $\adv$
        \item Receive $(\abort, p(\cdot))$ from $\adv$.
        \item If $\abort = 1$ or $deg(p) > t$:
        \item \qquad  Send $\bot$ to all parties.
        \item Otherwise:
        \item \qquad Set $\PK' \gets \secretComm \cdot g^{p(0)}$
        \item \qquad Set $\SK_i' \gets \SK_i + p(i)$ and  $\PK_i' \gets \PK_i\cdot g^{p(i)} $for all $i \in \set{n}$.
        \item Send $(\PK', \{ \PK_i' \}_{i \in \set{n}}, \SK_i')$ to each party $i \in \set{n}$
      \end{enumerate}
    \end{center}
  \end{framed}
    \caption{Ideal functionality for key generation allowing adversarial abort,
    as defined by Kate et al.~\cite{KMSSSY26}.
    }
  \label{fg:keygen-abort}
\end{figure}

\subsection{Online-Extractable Zero Knowledge Proof System}
\label{def:online-extractable}
Our protocol requires a proving system w.r.t. the following relation (discrete logarithm knowledge)

\[
R_{pok} = \{ (g, \mathrm{PK}, (\mathrm{sk})) : \mathrm{PK} = g^{\mathrm{sk}} \}
\]

For which we define the following ideal functionality we are trying to achieve

\begin{figure}[H]
\centering
\fbox{
\parbox{0.95\linewidth}{
\textbf{Ideal funtionality $\mathcal{F}^{\pk}_{zk}$}

\medskip

\textbf{Setup:} On input $\delta$, where $\delta$ is some constant indicating a timeout, initialize the functionality by performing the following steps:
\begin{enumerate}
    \item Initialize $Q_K^{(1)} \leftarrow \emptyset$, $Q_K^{(2)} \leftarrow \emptyset$.
    \item Generate public parameters $(E(\mathbb{F}_p), p, G, s, g) \leftarrow \textsf{GroupGen}(1^\lambda)$.
    \item Sample $h \xleftarrow{\$} G$.
    \item Set $\textsf{par} \leftarrow (E(\mathbb{F}_p), p, G, s, g, h)$.
\end{enumerate}

\textbf{KeyGen:} On query with input identifier $i'$ from party $i$.
\begin{enumerate}
    \item Output $\perp$ if $i'\neq i$.
    \item Output $\perp$ if $Q_K^{(1)}[i] \neq \perp$.
    \item Else, generate $\textsf{sk} \xleftarrow{\$} \mathbb{Z}_s$ and $\textsf{PK} \leftarrow g^{\textsf{sk}}$.
    \item Set $Q_K^{(1)} \leftarrow Q_K[i] = \textsf{PK}$.
    \item Output $(\textsf{sk}, \textsf{PK})$.
\end{enumerate}

\textbf{Register:} On query with public key $\textsf{PK}$, secret key $\textsf{sk}$, and identifier $i'$ from party $i$.
\begin{enumerate}
    \item Output $\perp$ if $i'\neq i$.
    \item Output $\perp$ if $Q_K^{(2)}[i] \neq \perp$.
    \item Output $\perp$ if $(\textsf{PK}, \textsf{sk})$ do not satisfy the relation $\mathcal{R}_P$, where
    \[
    \mathcal{F}^{\pk}_{zk} = \{(g, \textsf{PK}, (\textsf{sk})) : \textsf{PK} = g^{\textsf{sk}}\}.
    \]
    \item Else, set $Q_K^{(2)}[i] = \textsf{PK}$.
\end{enumerate}

\textbf{Query:} On query with no input, wait $\delta$ amount of time (where $\delta$ is the constant defined above), to give all parties enough time to complete registration. Then set
\[
B = Q_K^{(2)},
\]
such that
\[
B = \{(j, \textsf{PK}_j)\}_{j \in C}, \text{ where } C \subseteq [n].
\]
}
}
\caption{Ideal functionality $\mathcal{F}^{\pk}_{zk}$ for a public-key infrastructure with proof of possession of the corresponding secret key $\textsf{sk}$. Public parameters $\textsf{par}$ are given implicitly to all algorithms.}
\end{figure}




Proving the security of our protocol requires realizing $\mathcal{F}^{\pk}_{zk}$ which in our case requires
an online-extractable zero-knowledge proof system (in the UC framework rewinding is not allowed).

One possible direction is to use zero-knowledge proofs described in \cite{GOLDEN}.
The concrete relation following relation, $\mathcal{R}_P$,
demonstrates that an ElGamal ciphertext is correct.
The prover shows either that it knows the plaintext $\sk$ of an encryption,
or that it knows the discrete logarithm of a public value $h$ chosen at random
from $\mathbb{G}$ at the time of setup.
The second proof demonstrates by inference that the prover knows $\sk$,
if the discrete logarithm problem is hard in $\mathbb{G}$.

$$
  \mathcal{R}_P = \left\{ \left(g, h, \PK, c = (C_0, C_1), z \right) ; (\mathsf{sk}, r) \right\} \quad \text{iff}\\
$$
\begin{align*}
  &(1) \quad C_0 = g^r; \quad C_1 = h^r \cdot g^{\mathsf{sk}} \\
  &(2) \quad g^z = C_0 \cdot \PK^{c'} \\
  &(3) \quad h^z = \left(C_1 \cdot \PK^{-1}\right) \cdot \left(h^{\mathsf{sk}}\right)^{c'}
\end{align*}

The elements of the relation are as follows.
The witness is the pair $(\sk, r)$, where $\sk$ is the secret key and $r$ the encryption randomness.
The statement consists of the generator $g$ of $\Gr$, the random group element $h \rgets \Gr$ fixed at setup,
the public key $\PK = g^{\sk}$, the ElGamal ciphertext $c = (C_0, C_1)$ of $g^{\sk}$ under randomness $r$, and the response $z$.
Equation~(1) is the verifiable-encryption component that makes the proof online-extractable:
the simulator samples $h = g^{\eta}$ at setup, keeps the trapdoor $\eta$, and decrypts $c$ to recover the witness without rewinding.
Rather than transmit fresh Schnorr commitments, we reuse the encryption randomness $r$ as the proof nonce, so the
commitments are read off the ciphertext, namely $R_g \coloneqq C_0$ and $R_h \coloneqq C_1 \cdot \PK^{-1} = h^r$;
this binds the ciphertext to the proven statement and removes two redundant group elements.
The challenge $c'$ is \emph{not} provided by the prover: following the Fiat--Shamir transform,
the verifier derives it directly as
\[
  c' = \HashSchnorrR(C_0, C_1, \PK, \gamma),
\]
where the hash binds the ciphertext $(C_0, C_1)$, the statement $\PK$, and the proof context $\gamma$
(e.g.\@ $\gamma = (\sid, k, C_k[0])$ in the protocol).
The honest prover then sets the single response $z = r + c' \cdot \sk$, which simultaneously opens the discrete
logarithm of $\PK$ and certifies that $c$ encrypts $g^{\sk}$.

A practical construction that we will be using Figure~\ref{fg:concrete-simulator},
where the proof $\pi$ is practically implemented using Schnorr proofs.

\begin{figure}[H]
  \begin{framed}
    \begin{center}

\begin{itemize}
\item \textbf{Setup:} Accepts as input public parameters $\mathsf{par}$, and a timeout constant $\delta$.
\begin{enumerate}
    \item Initialize $Q_K^{(1)} \leftarrow \emptyset$, $Q_K^{(2)} \leftarrow \emptyset$.
    \item Generate public parameters $(\mathbb{F}_p, p, G, s, g) \leftarrow \mathsf{GroupGen}(1^\lambda)$.
    \item Sample $h \leftarrow_{\$} G$.
    \item Set $\mathsf{par} \leftarrow (\mathbb{F}_p, p, G, s, g, h)$.
\end{enumerate}

\item \textbf{KeyGen:} If $Q_K^{(1)}[i] \neq \perp$, return $\perp$.  
Else, sample $\mathsf{sk} \leftarrow_{\$} \mathbb{Z}_s$, set $\mathsf{PK} \leftarrow g^{\mathsf{sk}}$.  
Set $Q_K^{(1)}[i] = \mathsf{PK}$. Output $(\mathsf{PK}, \mathsf{sk})$.

\item \textbf{Register:} Accepts as input public key $\mathsf{PK}$, identifier $i'$ from party $i$, and proof $\pi$.
\begin{enumerate}
    \item Output $\perp$ if $i' \neq i$.
    \item Output $\perp$ if $Q_K^{(2)}[i] \neq \perp$.
    \item Output $\perp$ if $\mathsf{Verify}(\pi, \mathsf{PK}) \neq 1$, where $\pi$ proves the relation $\mathcal{R}_P$, as follows:
\end{enumerate}

\[
\mathcal{R}_P =
\left\{
\big( g, h, \mathsf{PK}, c = (C_0, C_1), z \big) ; (\mathsf{sk}, r)
\right\}
\quad \text{iff}
\]


\begin{align*}
(1)\quad & C_0 = g^r; \quad C_1 = h^r \cdot g^{\mathsf{sk}} \\
(2)\quad & g^z = C_0 \cdot \PK^{c'} \\
(3)\quad & h^z = (C_1 \cdot \PK^{-1}) \cdot (h^{\mathsf{sk}})^{c'}
\end{align*}

Here the witness is $(\sk, r)$; the statement comprises the generator $g$, the setup element $h \rgets \Gr$,
the public key $\PK = g^{\sk}$, the ElGamal ciphertext $c = (C_0, C_1)$, and the response $z$.
Equation~(1) is the verifiable-encryption component enabling online extraction (decrypt $c$ with the
setup trapdoor $\eta = \log_g h$). The Schnorr commitments are not sent separately but reused from the
ciphertext, $R_g \coloneqq C_0$ and $R_h \coloneqq C_1 \cdot \PK^{-1}$, binding the ciphertext to the statement.
The challenge $c'$ is not part of the proof: the verifier recomputes it directly via Fiat--Shamir as
\[
  c' = \HashSchnorrR(C_0, C_1, \PK, \gamma),
\]
binding the ciphertext $(C_0, C_1)$, the statement $\PK$, and the proof context $\gamma$; the honest prover
sends the single response $z = r + c' \cdot \sk$.

\begin{enumerate}
\setcounter{enumi}{3}
    \item Else, set $Q_K^{(2)}[i] = \mathsf{PK}$.
\end{enumerate}

\item \textbf{Query:} Wait $\delta$ amount of time (where $\delta$ is the constant defined above), to give all parties enough time to complete registration. Then, output $Q_K^{(2)}$.
\end{itemize}

    \end{center}
  \end{framed}
    \caption{Concrete protocol realizing $\mathcal{F}^{\pk}_{zk}$.
    }
  \label{fg:concrete-simulator}
\end{figure}
